import { Inter } from "next/font/google";
import { Lusitana } from "next/font/google";
export const inter = Inter({ subsets: ["latin"], preload: false });
export const lusitina = Lusitana({ subsets: ['latin'], weight: ["400"], preload: false });

